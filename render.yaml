services:
  - type: web
    name: tax-comparison
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run tax_comparison_app.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true
    plan: free
